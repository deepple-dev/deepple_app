// RequestOptions.extra keys used by Dio interceptors.
//
// Keep these keys centralized so app code and tests don't drift.

const String requiresAccessTokenExtraKey = 'requiresAccessToken';
const String retryExtraKey = 'retry';
const String skipAuthExtraKey = 'skipAuth';
